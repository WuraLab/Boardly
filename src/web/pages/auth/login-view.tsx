import Head from 'next/head';

const Home: React.FC = () => {
    return (
        <div>
            <Head>
                <title>Boardly</title>
            </Head>
            <div>Welcome to Boardly</div>
        </div>
    );
};

export default Home;
